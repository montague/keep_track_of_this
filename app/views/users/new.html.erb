<div style="margin-bottom:4px;">
	<h1 class="inline">Sign up</h1>
	<span>
		(or <%= link_to "sign in", signin_path %>)
	</span>
</div>
<%= form_for(@user) do |f| %>
	<%= render 'fields', :f => f %>
	<div class="actions">
		<%= f.submit "Sign up" %>
	</div>
<% end %>